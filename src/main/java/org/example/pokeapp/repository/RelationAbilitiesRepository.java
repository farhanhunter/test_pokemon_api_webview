package org.example.pokeapp.repository;

import org.example.pokeapp.entity.RelationAbilities;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RelationAbilitiesRepository extends JpaRepository<RelationAbilities, Long> {
}
